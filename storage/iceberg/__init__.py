"""Iceberg analytical store — pure Python, pyflink-free (work-unit 6.2, PR5).

Public submodules
-----------------
tables    -- create_training_event_table(catalog) → Table
sink      -- append_events(table, events) → None
compaction -- compact_table(table) → None
"""
